namespace ZLGCanComm.Enums;
public enum ReferenceCommand
{
    SetBaudRate = 1,                  // 设置波特率
    SetTransmitTimeout = 2,           // 设置传输超时时间
    AddFilter = 3,                    // 添加过滤器
    SetFilter = 4                     // 设置过滤器
}

